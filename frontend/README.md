# Frontend (Next.js + TypeScript)

RU-first workspace UI for AI Campaign market-ready MVP.

## Run

```bash
npm install
npm run dev
```

## Required env

- `NEXT_PUBLIC_API_BASE_URL` - backend URL (default `http://localhost:8000`)


